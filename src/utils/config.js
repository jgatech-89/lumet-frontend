/**
 * URL base del backend (origen, sin path).
 * Se obtiene de la variable de entorno VITE_API_URL para poder cambiar por entorno.
 * En build: definir VITE_API_URL (ej. https://api.tudominio.com).
 */
export const api = 'https://18.223.117.138.nip.io';