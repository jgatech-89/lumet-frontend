# URL base del backend (origen, sin path final).
# En desarrollo local: http://localhost:8000
# En producción: https://api.tudominio.com
# Vite expone solo variables que empiezan por VITE_
# VITE_API_URL=http://localhost:8000
